import App from "./App.vue"; // should be able to import files vue components
import { test } from "./index"; // should be able to import regular modules
window.document; // should be able to access web APIs
import { describe, it } from "node:test" // should be able to import from node